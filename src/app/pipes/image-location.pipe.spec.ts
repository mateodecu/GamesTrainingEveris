import { ImageLocationPipe } from './image-location.pipe';

describe('ImageLocationPipe', () => {
  it('create an instance', () => {
    const pipe = new ImageLocationPipe();
    expect(pipe).toBeTruthy();
  });
});
